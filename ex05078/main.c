#include <stdio.h>

/**
 * Always Remember!
 * When a float value is used for calculation,
 * %f character specifier should be used.
 */
int main()
{
	printf("The total is %f.\n", 16.0+17);
	return(0);
}
