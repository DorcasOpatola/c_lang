#include <stdio.h>

/**
 * @puts(): is a function that sends text to standard output,
 * it also prints the text on a new line.
 */
int main()
{
	puts("Hickory, dickory, dock,");
	puts("The mouse ran up the clock.");
	puts("The clock struck one,");
	puts("The mouse ran down,");
	puts("Hickory, dickory, dock.");
	return(0);
}
